---
title: "Dashboard and search"
description: "Turn saved list filters into widgets, and jump to parts, customers, vehicles, or jobs with Ctrl+K."
---

**Dashboard** is a command board you build, not a fixed report pack. Subtitle: build your command center from saved table filters.

Empty dashboard copy: **No widgets yet. Open a list view, apply filters, then click Add to Dashboard.**

That button is on Customers, Vehicles, Sales Orders, Workshop Orders, Inventory, Vendors, Purchase Orders, and Purchase Bills.

<Steps>
  <Step title="Filter a list the way you work">
    Search and sort until the table matches “open POs” or “today’s jobs.”
  </Step>
  <Step title="Add to Dashboard">
    Click **Add to Dashboard**. Name the widget. Choose list or a grouped metric.
  </Step>
  <Step title="Open Dashboard">
    Sidebar **Dashboard**. Widgets stay live as other people receive stock or update jobs.
  </Step>
</Steps>

## Global search

**Ctrl+K** (Windows) or **Cmd+K** (Mac) opens search. Placeholder: **Search parts, customers, vehicles, jobs…**

Search runs across five groups in parallel:
- **Commands**: Jump directly to views or trigger create dialogs (for example, **Open Workshop Intake** or **Create Sales Order**).
- **Customers**: Search by individual name, company name, phone, or email to open `/customers/:id`.
- **Vehicles**: Search by license plate, VIN, or make/model to open `/vehicles/:id`.
- **Workshop orders**: Search by work order number (e.g. `WO-2026-0004`), vehicle plate, or customer to open `/workshop/orders/:id`.
- **Inventory**: Search by SKU or description to jump to the part's stock ledger.

<Tip>
Lists stay live. When someone else receives stock or updates a job, the widgets update.
</Tip>

